services:
  webapp:
    build: ./facility
    volumes:
      - .:/facility
    ports:
      - "8013:8013"
    command: sh -c "pwd && python3 manage.py makemigrations && python3 manage.py makemigrations todo && python3 manage.py migrate && gunicorn facility.wsgi:application --bind 0.0.0.0:8013"
